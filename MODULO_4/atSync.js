//Exercicios de Fixação - Módulo 4 - Assíncrono

// #region Atividade 1 — Callback - Dada a Função chamar a função e exibir o resultado.

/**
 * function processarPedido(pedido, callback) {

    console.log(`Processando pedido ${pedido}...`);

    setTimeout(() => {
        callback(`Pedido ${pedido} processado!`);
    }, 2000);

}
 */

// #endregion

// #region Atividade 2 — Promise
/**
 * Crie uma função verificarEstoque(produto) que retorna uma Promise. 
 * A função deve receber um objeto produto com as propriedades nome e quantidade.
 *  Se a quantidade for maior que 0, a Promise deve ser resolvida 
 * com a mensagem "Produto em estoque". 
 * Caso contrário, a Promise deve ser rejeitada com a
 *  mensagem "Produto em falta."
 */

//#endregion

// #region Atividade 3 — async/await

/**
 * Criar uma função consultarProduto() que utiliza async/await para
 *  buscar informações de um produto.
 *  utilizar try/catch para tratar erros.
 * 
 */
